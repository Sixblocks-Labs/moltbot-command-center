/** @type {import('next').NextConfig} */
const nextConfig = {
  // For connecting to your Tailscale-only gateway from the browser.
  // If you later need permissive CORS headers, add them here.
};

export default nextConfig;
